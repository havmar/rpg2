# RPG2 — agent instructions

The canonical project instructions live in `AGENTS.md`, so every coding
agent shares one source of truth. Claude Code loads them through the import
below; Codex CLI (and other AGENTS.md-aware agents) read `AGENTS.md`
directly. **Edit `AGENTS.md`, not this file.**

@AGENTS.md
