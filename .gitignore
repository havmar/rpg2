__pycache__/
*.pyc
*.pyo
.notes.txt
scratch/

# Per-playthrough runtime state (not source)
# NOTE: the four player-facing ui/*.txt pages (party, map, fight-short, and
# fight-detailed) are NOT ignored -- `sheet` commits them so the player and DM
# can read the playthrough on the branch (see dm.md). save.json stays
# runtime-only.
save.json

# Legacy combat workfiles from before the last-fight .txt snapshots.
fight.log
ui/fight.log
